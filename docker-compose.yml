version: '2.4'

services:
#  server:
#    image: ghcr.io/loicviennois/the-binary-game-server
#    build: ./server
#    ports:
#      - '3000:3000'
  client:
    image: ghcr.io/loicviennois/the-binary-game-client
    build: ./client
    ports:
      - '8080:80'
